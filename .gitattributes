# Shell-/Config-Dateien MÜSSEN LF behalten — CRLF bricht bash (Zeilenfortsetzung,
# Heredocs) auf dem Linux-VPS. Verhindert die CRLF-Falle bei `git archive`-Deploys.
*.sh        text eol=lf
*.mjs       text eol=lf
Caddyfile*  text eol=lf
*.service   text eol=lf
*.timer     text eol=lf
deploy/**   text eol=lf
